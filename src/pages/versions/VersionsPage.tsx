export function VersionsPage() {
  return (
    <section className="panel">
      <h2>Version chain</h2>
      <p className="empty-state">Select a file from a result view to inspect its version chain.</p>
    </section>
  );
}

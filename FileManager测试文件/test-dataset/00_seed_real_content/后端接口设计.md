# 后端接口设计

需要提供 get_tag_page_data(tag_id, mode)。

structure 模式返回：当前 tag、直接子 tag、直接文件。
aggregation 模式返回：当前 tag、直接子 tag、直接文件、后代 tag、聚合文件、matchedTags、去重后的 totalAggregateFileCount。

关键词：get_tag_page_data、directFiles、aggregateFiles、matchedTags、SQLite、Tauri command。
